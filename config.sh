git config --global user.name "zmzu"
git config --global user.email "rahifmanjatha@yahoo.com"
git config --global core.editor vim
git config --global credential.helper store
echo "https://zmzu:$password@github.com" > ~/.git-credentials
