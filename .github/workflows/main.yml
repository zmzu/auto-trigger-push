on: 
  schedule:
    - cron: '* * * * *'
  push:

jobs:
   name: auto push
   runs-on: ubuntu-20.04
   env:
     password: ${{ secrets.PASSWORD }}
   steps:
   - uses: actions/checkout@v3
   - name: git config
     run: |
       ./config.sh
   - name: bmake
     run: |
       ./bmake.sh
