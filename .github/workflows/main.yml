on: 
  schedule:
    - cron: '* * * * *'
  push:

jobs:
  job1:
    name: auto push
    runs-on: ubuntu-latest
    env:
      password: ${{ secrets.PASSWORD }}
    steps:
    - uses: actions/checkout@v3
    - name: git config
      run: |
        ./config.sh
    - name: bmake
      run: |
        ./bmake.sh
